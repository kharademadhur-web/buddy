# Frontend environment variables (copy to .env and fill values)

# Google OAuth 2.0 client ID (enable origins for http://localhost:5173; add your dev origin)
VITE_GOOGLE_CLIENT_ID="your-google-client-id-here"

# Backend API base URL (where your backend is running)
VITE_API_BASE="http://localhost:8000"

# Optional: Additional model or feature flags
# VITE_ENABLE_HISTORY=false