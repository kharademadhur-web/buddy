services:
  - type: web
    name: buddy-backend
    runtime: python
    pythonVersion: 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: OPENROUTER_API_KEY
        scope: build
      - key: SECRET_KEY
        scope: build
      - key: DATABASE_URL
        value: sqlite:///./buddy.db