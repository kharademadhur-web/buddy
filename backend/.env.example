# Copy to .env and fill values
GOOGLE_CLIENT_ID=
SECRET_KEY=change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALLOWED_ORIGINS=http://localhost:5173,https://buddy-frontend.kharademadhur.workers.dev

# Optional demo mode for development
# Set to "true" to allow unauthenticated access with a demo user in dev mode
ALLOW_DEMO_AUTH=false

# Development mode flag (optional)
# ENV=dev
